<?xml version="1.0" encoding="UTF-8"?>
<StackLayout xmlns="http://xamarin.com/schemas/2014/forms" 
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
    x:Class="ibanking.Controls.CoopInfoBtn"
    Orientation="Horizontal"
    xmlns:controls="clr-namespace:ibanking.Controls;assembly=ibanking">

    <Frame CornerRadius="5" Padding="10, 10, 10, 10" Style="{DynamicResource CoopinfoButton}" 
        VerticalOptions="Start"
        x:Name="main"
        HasShadow="false"
        HorizontalOptions="FillAndExpand">
       <StackLayout HorizontalOptions="FillAndExpand"
            VerticalOptions="FillAndExpand"
            Orientation="Horizontal">
             <Label 
                x:Name="ButtonText"
                TextColor="White"
                HorizontalOptions="FillAndExpand"></Label>
             <controls:IconLabel TextColor="White" Text="&#xf04b;"></controls:IconLabel>
        </StackLayout>
    </Frame>

</StackLayout>


